<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/butonsmrtvouno"
        android:label="@string/app_name"
        android:roundIcon="@drawable/butonsmrtvouno"
        android:supportsRtl="true"
        android:theme="@style/Theme.RadioTvOnline"
        tools:targetApi="31">

        <activity android:name=".MainActivity">
            <!-- Add any intent filters or other configurations if needed -->
        </activity>

        <activity android:name=".am">
            <!-- Add any intent filters or other configurations if needed -->
        </activity>

        <activity android:name=".fm">
            <!-- Add any intent filters or other configurations if needed -->
        </activity>

        <activity android:name=".tv">
            <!-- Add any intent filters or other configurations if needed -->
        </activity>


        <activity
            android:name=".slash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>